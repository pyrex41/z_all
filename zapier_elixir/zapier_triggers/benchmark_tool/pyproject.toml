[project]
name = "zapier-triggers-benchmark"
version = "1.0.0"
description = "Performance benchmark tool for Zapier Triggers API"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
]

[project.scripts]
benchmark = "benchmark:main"
